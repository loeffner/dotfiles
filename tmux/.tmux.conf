# Configure tpm
run-shell 'tmux set -g @DOTFILES_DIR "$DOTFILES_DIR"'
run-shell 'tmux set-environment -g TMUX_PLUGIN_MANAGER_PATH "$DOTFILES_DIR/tmux/plugins"'

# Add plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'egel/tmux-gruvbox'
set -g @plugin 'tmux-plugins/tmux-yank'

# Mouse support
set -g mouse on

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-option renumber-windows on

# Colors & Theme
set-option -sa terminal-overrides ",xterm*:Tc"
set -g @tmux-gruvbox-right-status-y '%d.%m.%Y'
set -g @tmux-gruvbox-right-status-x '%H:%M'

# Set vi mode
set-window-option -g mode-keys vi

# Key Rebinds
# Remap prefix from 'C-b' to 'C-Space'
unbind C-b
set-option -g prefix C-Space
bind-key C-Space send-prefix

# Reload config file
bind r source-file ~/.tmux.conf

# Split panes
bind-key \\ split-window -h
bind-key - split-window -v

# Keybindings
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel
bind "-" split-window -v -c "#{pane_current_path}"
bind "\\" split-window -h -c "#{pane_current_path}"


# Run tpm
run '$DOTFILES_DIR/tmux/plugins/tpm/tpm
